spring:
  datasource:
    url: jdbc:postgresql://10.178.0.12:5432/postgresql
#    url: jdbc:postgresql://localhost:5432/postgresql
    username: postgresql
    password: postgrespassword
  jpa:
    show-sql: true
    hibernate:
      dialect: org.hibernate.dialect.PostgreSQLDialect
      ddl-auto: update

  rabbitmq:
    host: 10.178.0.14
    username: guest
    password: guest
    port: 5672